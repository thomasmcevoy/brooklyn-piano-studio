---
const { size } = Astro.props;
---

<div class={`container ${size}`}>
  <slot />
</div>

<style>
.container {
  width: min(100% - clamp(2.5rem, 10vw, 6rem), var(--max-width));
  margin-inline: auto;
  padding-inline: 0;
}

.wide {
  --max-width: 64rem;
}

.medium-flexible {
  --max-width: clamp(400px, 75vw, 1000px);
}

.narrow {
  --max-width: 60ch;
}
</style>